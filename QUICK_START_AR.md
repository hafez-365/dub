# دليل البدء السريع - تشغيل الواجهة فقط

## الخطوة 1: تثبيت المتعلقات

```bash
cd /path/to/dub
pnpm install
```

## الخطوة 2: التأكد من ملف البيئة

تم إنشاء ملف `.env.local` بالفعل في `apps/web/`:
- جميع الخدمات الخارجية معطلة
- لن يتم محاولة الاتصال بأي خوادم خارجية

## الخطوة 3: تشغيل التطبيق

```bash
pnpm dev
```

ستتم طباعة رسالة مثل:
```
▲ Next.js 15.5.8
- Local:        http://localhost:8888
```

## الخطوة 4: فتح المتصفح

انسخ الرابط وافتحه في متصفحك:
```
http://localhost:8888
```

---

## المشاكل الشائعة والحلول

### 🔴 خطأ: "DATABASE_URL is required"
```
✅ حل: هذا طبيعي! التطبيق يحاول الاتصال بقاعدة بيانات
    - يمكنك تجاهل الخطأ واستمرار الاختبار
    - الواجهة ستعمل بدون بيانات فعلية
```

### 🔴 خطأ: "prisma generate" لم ينجح
```
✅ حل: شغل:
    pnpm prisma:generate
```

### 🔴 الصفحة تعطيها خطأ "500"
```
✅ حل: 
   1. انظر إلى console (F12 في المتصفح)
   2. اضغط Ctrl+Shift+R لتنظيف الـ cache
   3. جرب صفحة أخرى
```

### 🔴 المنفذ 8888 مشغول
```
✅ حل: استخدم منفذ مختلف:
    PORT=3000 pnpm dev
```

---

## ماذا يمكنك أن تختبر؟

✅ **يعمل:**
- استكشاف الواجهة والتصميم
- التنقل بين الصفحات
- عرض المكونات الثابتة
- الأنماط والألوان

❌ **لا يعمل:**
- إنشاء حسابات جديدة (بدون قاعدة بيانات)
- إنشاء روابط قصيرة
- إدارة الفريق
- الدفع
- الإحصائيات

---

## الخطوة التالية

بعد الانتهاء من اختبار الواجهة:

### أضف Supabase (اختياري)

1. أنشئ حساب مجاني على https://supabase.com
2. أنشئ مشروع جديد
3. انسخ Connection String
4. الصقه في `.env.local`:
   ```env
   DATABASE_URL="your-supabase-url"
   ```
5. شغل:
   ```bash
   pnpm prisma:push
   ```

---

## ملفات مهمة

```
apps/web/
├── .env.local              ← إعدادات البيئة (معدة بالفعل)
├── app/                    ← صفحات التطبيق
│   ├── page.tsx           ← الصفحة الرئيسية
│   └── ...
├── components/            ← المكونات
└── lib/
    └── disable-external-services.ts ← معالج تعطيل الخدمات
```

---

**أسئلة؟ تحقق من README.md أو زر docs.dub.co**
